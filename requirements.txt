# Cài đặt: python -m pip install -r requirements.txt
#
# LƯU Ý VỀ urllib3:
#   pytrends 4.9.2 gọi urllib3 Retry(method_whitelist=...) - tham số đã bị xóa ở urllib3 2.x.
#   Không sao, vì code trong trends/client.py CỐ TÌNH không truyền retries= vào TrendReq
#   (đã có vòng retry riêng ở trends/fetcher.py). Nên urllib3 2.x vẫn chạy tốt.

pandas>=2.0
openpyxl>=3.1
pytrends>=4.9.2
requests>=2.31

# --- CHỈ CẦN KHI DÙNG GIAO DIỆN WEB (Chay_giao_dien_web.bat) ----------------
# Giao diện cửa sổ Windows (Chay_giao_dien.bat) KHÔNG cần hai dòng này — nó
# dùng tkinter có sẵn trong Python. Thiếu hai dòng này thì chỉ riêng giao diện
# web không mở được, mọi thứ còn lại chạy bình thường.
fastapi>=0.115
uvicorn>=0.30

# --- TÙY CHỌN: chỉ cần khi dùng Claude để viết bài ---------------------------
# Công cụ viết bài mặc định dùng Google Gemini (miễn phí) qua REST nên KHÔNG
# cần thư viện nào thêm. Chỉ khi đổi NHA_CUNG_CAP=claude trong file .env mới
# cần dòng dưới. Bỏ dấu # rồi cài lại:
#     python -m pip install -r requirements.txt
#
# anthropic>=0.40
